---
  - name: test docker login from remote server
    hosts: app_servers

    tasks:
      - name: take massage from docker login
        shell: "cat ~/secrets/.docker_push_secret | docker login -u {{ gh_user }} --password-stdin"
        register: result 

      - name: display message result
        debug:
          msg: "{{ result }}" 